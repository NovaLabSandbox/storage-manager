@inherits LayoutComponentBase
<Layout>
    <Header Class="header">
        <Menu Theme="MenuTheme.Dark" Mode="MenuMode.Horizontal" DefaultSelectedKeys=@(new []{"2"})>
            <MenuItem Key="1" RouterLink="" RouterMatch="@NavLinkMatch.Prefix">Site</MenuItem>
        </Menu>
    </Header>
    <Layout>
        @if (_siteState.Value.SelectedSite != null)
        {
         <Sider Width="200" Class="site-layout-background">
            <Menu Mode="MenuMode.Inline"
                  Theme=MenuTheme.Dark
                  Style="height: 100%; border-right: 0;">
                <MenuItem RouterLink=@($"/site/{_siteState.Value.SelectedId}") RouterMatch="@NavLinkMatch.Prefix" Icon="pie-chart">
			        <ChildContent>
				        Dashboard
			        </ChildContent>
		        </MenuItem>
                <MenuItem RouterLink=@($"/site/{_siteState.Value.SelectedId}/areas") RouterMatch="@NavLinkMatch.Prefix" Icon="pie-chart">
			        <ChildContent>
				        Areas
			        </ChildContent>
		        </MenuItem>
                <MenuItem RouterLink=@($"/site/{_siteState.Value.SelectedId}/devices") RouterMatch="@NavLinkMatch.Prefix" Icon="pie-chart">
			        <ChildContent>
				        Devices
			        </ChildContent>
		        </MenuItem>
@* 
                @{
                    RenderFragment sub2Title =
                    @<span>
                        <Icon Type="laptop" Theme="outline" />
                        subnav 2
                    </span>;
                }
                <SubMenu Key="sub2" Title=sub2Title>
                    <MenuItem Key="5">option5</MenuItem>
                    <MenuItem Key="6">option6</MenuItem>
                    <MenuItem Key="7">option7</MenuItem>
                    <MenuItem Key="8">option8</MenuItem>
                </SubMenu>
                @{
                    RenderFragment sub3Title =
                    @<span>
                        <Icon Type="notification" Theme="outline" />
                        subnav 3
                    </span>;
                }
                <SubMenu Key="sub3" Title=sub3Title>
                    <MenuItem Key="9">option9</MenuItem>
                    <MenuItem Key="10">option10</MenuItem>
                    <MenuItem Key="11">option11</MenuItem>
                    <MenuItem Key="12">option12</MenuItem>
                </SubMenu> *@
            </Menu>
        </Sider>
        }
        <Layout Style=" padding: 0 24px 24px;">
            <Breadcrumb Style="margin: 16px 0;">
                <BreadcrumbItem>Home</BreadcrumbItem>
                <BreadcrumbItem>List</BreadcrumbItem>
                <BreadcrumbItem>App</BreadcrumbItem>
            </Breadcrumb>
            <Content Class="site-layout-background" Style=" padding: 24px; margin: 0; min-height: 280px;">
                @Body
            </Content>
        </Layout>
    </Layout>
</Layout>

<style>
    .site-layout-background {
        background: #fff;
    }
</style>
